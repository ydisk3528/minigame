# Temple Orb Match Level Editor

```powershell
cd "E:\wg\cocos\minigame\Temple Orb Match\level-editor"
npm install
npm run dev
```

Use **Write To Folder** and select `assets/resources/levels` to save generated levels and `catalog.json` directly into the game project. Run `npm run catalog` after manually adding or removing level files.
