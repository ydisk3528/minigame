import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

@ccclass('UIManager')
export class UIManager extends Component {
    // Screen-level UI switching is implemented with the UI milestones.
}
