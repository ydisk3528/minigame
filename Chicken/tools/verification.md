# 验证记录

2026-09-12，Cocos Creator 3.8.6，Windows。

- TypeScript 项目检查通过。
- 下载清单 146 项全部成功，SHA256 与本地文件一致。
- 已生成预制体的资源 UUID、SpriteFrame 及节点层检查通过；无重复 UUID。
- 15 组 Spine 的导入数据包含有效纹理引用。
- 规则回归：扣款、碰撞、两位小数、重复结算、待领取奖励恢复、奖金封顶、三档难度、存档校验和桥接回退。
- 控制器回归：真实预制体节点绑定、所有面板路径、连点、失败重试、胜负面板、转盘单次结算、自动游戏停止、启动重试。
- 本地浏览器已观察到实际渲染的小鸡和车辆、首轮成功过路、路障及兑现面板；透明贴图与初始空回合问题已修复。
- 390 × 844 视口下检查了完整主界面、顶部步数/倍率、道路裁切和自动设置面板。
- 浏览器实际运行了自动跳跃、达到目标后兑现及碰撞失败，检查期间未捕获游戏控制台错误。
- 自动游戏结算面板增加停止入口；失败面板使用循环待机骨骼，避免碰撞动画播放完后小鸡消失。
- 最终浏览器检查：难度菜单显示 EASY/MEDIUM/HARD；选 HARD 后顶部切换为 20 步；下注菜单选 25 后实际扣款 25，首步兑现值为 35.25。继续跳跃触发失败后，结果面板小鸡保持可见。
- 最终构建成功；main 包 25,633 字节，整个 Web 输出 13,329,173 字节（未压缩磁盘大小）。

原站试玩标签曾失去响应并占用大量内存，后续资源分析使用下载文件，未继续打开原站。原站与本工程之间未完成逐像素截图对照。

未验证：Android 真机 Native 桥接、生产服务器部署及弱网实测；原站服务器规则不在恢复范围内。

## 横屏适配

- 设计尺寸统一为 1136 × 640，启动脚本、场景相机、Canvas 和 Web landscape 构建设置同步。
- 主玩法及全部 5 个可运行预制体重新排版，保留已有 SpriteFrame、骨骼和按需加载。
- 道路起始背景扩展到横屏左边界；移除会覆盖静态道路坐标的 Layout 组件，前进动画与横屏坐标保持一致。
- TypeScript、实际预制体控制器回归、资源引用检查与 Web 构建通过。
- 浏览器检查了默认桌面横屏和 844 × 390 手机横屏视口，主界面、自动设置与规则面板可见。
- 最终 844 × 390 预览实际完成跳跃和兑现：小鸡落在对应车道，35.25 结算金额、角色和继续按钮完整显示，未捕获控制台错误。
- 手机浏览器方向锁定和安全区域仍需 Android/iOS 真机验证；桌面视口验证不等于真机验证。
