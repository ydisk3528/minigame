import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

@ccclass('EventManager')
export class EventManager extends Component {
    // Typed game events are introduced as systems become active.
}
